class Personnes {
    constructor (nom, prenom, age) {
        this.nom = nom;
        this.prenom = prenom;
        this.age = age;
    }
}

export default Personnes // utiliser {} si plusieurs classes à exporter, séparer d'une "," et supprimer le "default"